<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
</head>
<body>
    <h1> <%= heading %></h1>
    <form action="<%= action %>" method="POST">
        <label>Name</label>
        <br/>
        <input type="text" name="name" value="<% if(editMode && employee.name) { %> <%=employee.name%> <% } %>">
        <br/><br/>

        <label>Email</label>
        <br/>
        <input type="email" name="email" value="<% if(editMode && employee.email) { %> <%=employee.email%> <% } %>">
        <br/><br/>

        <label>Designation</label>
        <br/>
        <input type="text" name="designation" value="<% if(editMode && employee.designation) { %> <%=employee.designation%> <% } %>">
        <br/><br/>

        <% if(editMode && employee && employee.id) { %>
            <input type="hidden" name="employeeId" value="<%=employee.id%>">
        <% } %>

        <input type="submit">
    </form>
</body>
</html>